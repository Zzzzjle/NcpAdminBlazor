@using NcpAdminBlazor.Client.Providers

<MudNavMenu Color="Color.Primary">
    @foreach (var menuItem in MenuProvider.MenuItems)
    {
        <NavItemRenderer Item="menuItem"/>
    }
</MudNavMenu>

@code {
    [Inject] private MenuProvider MenuProvider { get; set; } = null!;
}